# TODO make (a) nice plot function(s) to investigate the important properties of a model
# plotModel <- function(model){
#   pp_check(model)
#   plot(marginal_effects(model), points = T, point_args = list(alpha = 0.05))#, jitter_width = 0.25)
#   plot(model)
# }
